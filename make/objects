objects_library = $(call to_object,$(sources_library))

# Generate rules for object files and their dependency files
$(foreach source,$(sources_library),$(eval $(call generate_object_rule,$(source)) $(eval $(call generate_dependency_file_rule,$(source),to_object))))
