static_library := $(build_libraries_directory)/$(project).a
dynamic_library := $(build_libraries_directory)/$(project).so

$(call evaluate_library_rule,$(static_library),$(objects_static_library),static)
