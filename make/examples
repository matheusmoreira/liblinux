# Library usage examples
examples = $(basename $(notdir $(sources_examples)))

# Generate example executable rules for every example source file
$(foreach example,$(sources_examples),$(eval $(call generate_example_rules,$(example))) $(eval $(call generate_dependency_file_rule,$(example),example_to_executables)))
